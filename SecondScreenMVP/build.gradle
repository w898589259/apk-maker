// 项目级配置，管整个项目的编译插件版本
buildscript {
    dependencies {
        // 安卓Gradle插件，必须保留
        classpath 'com.android.tools.build:gradle:7.4.2'
        // Kotlin插件，必须保留
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.22"
    }
}
// 注意：这里不要加repositories！已经交给settings.gradle管了，加了就会冲突
